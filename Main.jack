class Main {
    function void main() {
        var Game game;
        var int seed;

        let seed = Player.getKeyPressed();
        
        do Menu.drawMenu();
        let game = Game.new(seed);
        do game.start();

        return;
    }
}